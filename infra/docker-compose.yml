# Docker is no longer required for FootConnect.
# - PostgreSQL is hosted on Supabase (free cloud tier)
# - Redis is hosted on Upstash (serverless cloud tier)
# Local development connects directly to cloud services.
name: footconnect
services: {}
